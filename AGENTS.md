# 工作区存储约束

- 本项目所有新增的用户数据、缓存、测试输出、临时文件和工具下载都必须保存在项目根目录的 `.workspace/` 内。
- 不要把项目数据写入 `C:\`、`%USERPROFILE%`、系统 `%APPDATA%`、系统 `%LOCALAPPDATA%` 或系统临时目录；只有用户另行明确授权时才可例外。
- 执行安装、运行、测试或构建命令前，优先通过 `scripts\workspace-env.bat`、`workspace-shell.bat` 或已经配置好的工作区终端加载项目环境。
- 不要删除工作区外的历史数据。若发现旧数据，先盘点并复制、验证，再请求用户确认是否清理。
- `.workspace/` 是生成目录，不应加入版本控制。
